<?xml version="1.0" encoding="utf-8"?>
<bundler>

	<find>
		select username, realname from users where username = ${param}
	</find>

	<replace>
		replace into users(username, password, realname) values
		(${param.username},
		${param.password}, ${param.realname})
	</replace>

	<delete>
		delete from users where username = ${param}
	</delete>

	<findAll>
		select username, realname from users limit ${params[1]} offset
		${params[0]}
	</findAll>

</bundler>